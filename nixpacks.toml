[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = [
  'npm run build',
  'echo "=== Checking for .medusa directory ==="',
  'ls -la .medusa/ || echo ".medusa directory not found!"'
]

[start]
cmd = 'npm run start'